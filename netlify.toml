[build]
  command = "npm install"  # 安装依赖
  publish = "."  # 发布根目录（因为是后端服务，无需静态文件）

[dev]
  command = "node index.js"  # 开发环境启动命令
  port = 3000

[build.environment]
  NODE_VERSION = "16"  # 指定 Node 版本，避免兼容性问题